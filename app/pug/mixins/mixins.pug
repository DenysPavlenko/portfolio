mixin times(limit)
  - for (var i = 1; i <= limit; i++) {
      block
  - }

mixin icon(name, className)
  svg(class=`svg-icon ${className ? className : ''}`)&attributes(attributes)
    use(href=`images/svg/svg-sprite/symbol/sprite.svg#${name}` x="0" y="0")
